---
const { id, className = "" } = Astro.props;
---

<section {id} class="-mt-36 pt-36">
<div class=`content-container border-sketch-${Math.round(Math.random() * 6)} bg-secondary-300 font-semibold px-8 md:px-24 py-6 md:py-8 text-lg ${className}`>
  <div class="flex flex-col gap-3 sm:gap-6">
    <slot />
  </div>
</div>
<section>
